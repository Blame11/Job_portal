spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/job-portal-test
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

server:
  port: 8080

jwt:
  secret: test-jwt-secret-key-2024
  expiration: 86400000

cookie:
  name: jobPortalToken
  secret: test-cookie-secret-key-2024

cors:
  allowed-origins: http://localhost,http://localhost:3000,http://localhost:5173

upload:
  dir: public/uploads/

logging:
  level:
    root: INFO
    com.jobportal: DEBUG
